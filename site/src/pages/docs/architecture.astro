---
import { getEntry, render } from 'astro:content';
import Docs from '../../layouts/Docs.astro';

const entry = await getEntry('docs', 'architecture');
if (!entry) throw new Error('Documento "architecture" não encontrado em src/content/');

const { Content } = await render(entry);
---
<Docs title="Como funciona — Toolbox" description="Entenda a estrutura do aplicativo e onde fica cada coisa.">
  <Content />
</Docs>
